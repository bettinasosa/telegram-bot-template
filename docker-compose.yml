version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram-bot
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - NODE_ENV=${NODE_ENV:-production}
    # Uncomment to mount logs directory
    # volumes:
    #   - ./logs:/app/logs
    # Uncomment if bot needs to connect to other services
    # networks:
    #   - bot-network

# Uncomment if needed
# networks:
#   bot-network:
#     driver: bridge

